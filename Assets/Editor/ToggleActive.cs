using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

namespace TwoWorldsEditor
{
	public static class ToggleActive
	{
		/*
		[MenuItem("GameObject/Toggle Active State %g")]
		public static void Toggle()
		{
			foreach(var go in Selection.gameObjects)
			{
				go.SetActive(!go.activeSelf);
			}
		}

		[MenuItem("GameObject/Toggle Active State %g", true)]
		public static bool ToggleValidate() => Selection.gameObjects.Length > 0;
		*/
	} 
}
